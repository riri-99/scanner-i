def run_scan(path: str):
    """Phase 1 stub — will hold real scanner logic later."""
    return {"status": "ok", "path": path}


def run_generate(path: str):
    """Phases 1-3 stub — will orchestrate everything later."""
    return {"status": "ok", "path": path}
